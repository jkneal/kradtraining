<config>

  <param name="rice.logging.configure">true</param>

  <param name="location.mode">LOCAL</param>
  <param name="coreservice.mode">LOCAL</param>
  <param name="ken.mode">LOCAL</param>
  <param name="kim.mode">LOCAL</param>
  <param name="kew.mode">LOCAL</param>
  <param name="krms.mode">LOCAL</param>
  <param name="ksb.mode">LOCAL</param>

  <param name="module.name">krtrain</param>
  <param name="application.id">krtrain</param>
  <param name="app.context.name">krtrain</param>
  
  <param name="filter.login.class">org.kuali.rice.krad.web.filter.DummyLoginFilter</param>
  <param name="filtermapping.login.1">/*</param>
  <param name="filterexclude.login">.*/kr-kradguest/.*</param>

  <param name="dev.mode">true</param>

  <param name="datasource.ojb.platform">MySQL</param>
  <param name="datasource.url">jdbc:mysql://localhost:3306/rice</param>
  <param name="datasource.username">RICE</param>
  <param name="datasource.password">RICE</param>

  <param name="transaction.timeout">3600</param>

  <param name="config.location">classpath:META-INF/common-config-defaults.xml</param>
  <param name="config.location">classpath:META-INF/common-config-locations.xml</param>
  <param name="config.location">${alt.config.location}</param>
  <param name="version">kradtraining :: ${project.version} :: ${kuali.build.timestamp}</param>

  <param name="resourceBundleNames">org.kuali.rice.krad.KRADApplicationResources,org.kuali.rice.krad.ApplicationResources,KrtrainApplicationResources</param>

  <param name="rice.krad.schema.packages">org.kuali.rice.krad.uif,org.kuali.rice.krad.datadictionary,org.kuali.rice.krad.lookup,org.kuali.rice.krtrain.util</param>
</config>
